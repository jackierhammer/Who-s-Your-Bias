// // this file will be used to seed the database

// const db = require('../config/connection');
// // a model for idol groups will need to be created
// const { Group } = require('../models');

// const groupData = require('./groupData.json');

// db.once('open', async () => {
//   await Group.deleteMany({});

//   const idolGroups = await Group.insertMany(groupData);

//   console.log('Group data seeded!');
//   process.exit(0);
// });
